name: Deploy

on:
  push:
    branches: [main]
    tags: ['v*']

jobs:
  deploy:
    if: github.repository != 'easydeploytest/template'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: easy-deploy dispatch
